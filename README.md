# marketpriceMagok

마곡동 상가·업무시설 실거래가를 건물별로 조회하는 Vite + React 대시보드입니다.

## 실행

```powershell
npm install
npm run dev
```

## 빌드

```powershell
npm run build
```

`docs/ai-output`의 정적 대시보드 산출물은 Vite 빌드 시 `dist`에 함께 복사됩니다.
